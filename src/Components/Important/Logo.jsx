// eslint-disable-next-line react/prop-types
const Logo = ({ css }) => {
  return (
    <p className={`font-neural text-2xl ${css}`}>
      Job<span className="text-main-v2">tile</span>
    </p>
  );
};

export default Logo;
